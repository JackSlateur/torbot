#
TOKEN = 'YOUR DISCORD APP TOKEN'
transmission_url = 'http://localhost:9091/torrent'

# If transmission is protected via basic auth, fill in the login:password
# Else, set it to None
transmission_auth = ('user', 'passwd')
